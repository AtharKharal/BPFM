---
title: "R Solution"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
```

A glimpse into the structure of the dataset is given below:

```{r}
dt  <- read.csv("Blood_Pressure_data.csv")
glimpse(dt)
```
```{r}
table(dt$weight)
```
```{r}
skimr::skim(dt)
```

